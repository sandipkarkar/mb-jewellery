/**
 * Matches `Header` / `Footer`: centered `container` + horizontal padding.
 * Do not add `max-w-full` here — it overrides the container’s responsive max-widths.
 */
export const PAGE_CONTAINER = "container mx-auto px-3 sm:px-4" as const;

export const PAGE_VERTICAL_PADDING = "py-6 sm:py-8 md:py-10" as const;
