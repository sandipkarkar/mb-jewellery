[build]
  command = "npm run build"
  publish = "dist"

# SPA: direct loads / refresh on client routes (e.g. /products) must serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
